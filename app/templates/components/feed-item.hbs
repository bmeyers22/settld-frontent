{{#if isTransaction}}
  <div class="item-type transaction">
    <i class="dollar icon"></i>
  </div>
{{else}}
  <div class="item-type job">
    <i class="briefcase icon"></i>
  </div>
{{/if}}
<div class="label image">
  {{user-image user=user}}
</div>
<div class="content">
  <div class="item-wrapper">
    <div class="info">
      <div class="summary">
        <div class="text">{{displayName}} {{actionName}} {{item.title}}</div>
        <div>
          <div class="icons">
            {{#if isTransaction}}
              {{#if pendingInvoice}}<i class="mini yellow clock icon"></i>{{/if}}
              {{#if rejectedInvoice}}<i class="mini red remove icon"></i>{{/if}}
              {{#if paidInvoice}}<i class="mini green check icon"></i>{{/if}}
            {{/if}}
          </div>
          {{fuzzy-time item.date}}
        </div>
      </div>
      <div class="extra text">
        {{item.description}}
      </div>
      <div class="meta">
        <a class="like">
          <i class="like icon"></i> 4 Likes
        </a>
      </div>
    </div>
  </div>
</div>
